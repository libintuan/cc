<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunset.grass.dao2.UserMapper">

    <insert id="save" parameterType="UserInfo">
        insert INTO user_info (name,city_id,sex,phone,email,password)
        values (
        #{name,jdbcType=VARCHAR},
        #{cityId,jdbcType=VARCHAR},
        #{sex,jdbcType=VARCHAR},
        #{phone,jdbcType=VARCHAR},
        #{email,jdbcType=VARCHAR},
        #{password,jdbcType=VARCHAR}
        )
    </insert>

    <select id="get" parameterType="UserInfo" resultType="UserInfo">
        select * from user_info where id=#{id}
    </select>
</mapper>